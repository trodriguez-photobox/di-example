package com.photobox.example.di.automatic_di

case class User(
                 firstName: String,
                 lastName: String,
                 email: String,
                 age: Int
               )
